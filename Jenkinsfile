pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    // Giả lập quá trình build
                    echo "Building..."
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    // Giả lập quá trình test
                    echo "Testing..."
                }
            }
        }
    }
}
